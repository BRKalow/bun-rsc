import path from "path";

export const BUILD_DIR = ".basket";
export const CLIENT_BUILD_DIR = path.join(BUILD_DIR, "client");
export const SERVER_BUILD_DIR = path.join(BUILD_DIR, "server");

export const CLIENT_MANIFEST_FILE = "react-client-manifest.json";
